# Countries

Search and browse country details using the REST Countries API.

## Concepts

- `useEffect` with external API calls
- Filtering and conditional rendering based on result count
- Separate components (`CountryFilter`, `CountryList`, `CountryDetails`)
- Error handling for failed fetches
